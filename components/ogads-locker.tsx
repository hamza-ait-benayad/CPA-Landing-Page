"use client"

/**
 * OGAds Content Locker Component
 * This component doesn't pre-load the script - it will be loaded only when download is clicked
 * The script will be loaded dynamically when triggerOGAdsLocker is called
 */
export default function OGAdsLocker() {
  // Don't load script on page load - only load when download button is clicked
  return null
}

